[package]
name = "fetchprofile"
version = "0.1.0"
edition = "2024"

[dependencies]
fetchlib = {path = "../fetchlib/"}
serde = {version = "1.0.228", features = ["std", "derive"]} 
serde_json = "1.0.149"

